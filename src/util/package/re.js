export const cut = /^\/+|\/+$/g;

export const js = /\.js$/;

export const name = /[^.]+/;

export const optim = /^([^\<\>.]*?)(?:\<([^\<\>.]*)\>)?([^\<\>]+)$/;

export const has = /[\\\/][^\\\/]*$/;

export const alias = /[^\\\/]+$/g;

export const isRoot = /^\w+\:[\\\/]+/;